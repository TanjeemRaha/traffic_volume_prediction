# Saved model

Step 3 writes `best_traffic_model.joblib` here. It contains the fitted
preprocessing pipeline, selected regressor, time origin, and training metadata.

Only load joblib files produced by code you trust; serialized model files can
execute code when loaded.

