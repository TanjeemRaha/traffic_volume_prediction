# Raw data

`python 01_download_and_clean.py` downloads the official UCI file here. The raw
CSV is intentionally excluded from Git because it is reproducible from the
documented source.

If automatic download is blocked, download UCI dataset 492 manually and place
`Metro_Interstate_Traffic_Volume.csv.gz` (or the uncompressed CSV) in this
folder, then run step 1 again.

